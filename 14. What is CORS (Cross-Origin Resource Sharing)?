Answer: 
CORS is a security feature implemented by web browsers that restricts web pages from making requests to a different domain than the one from which the page was loaded. 
It can be configured by the server to allow or deny cross-origin requests, typically through HTTP headers like Access-Control-Allow-Origin.
